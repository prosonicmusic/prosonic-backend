version: '3'

services:
  # prosinic:
  #   build: 
  #     context: .
  #     dockerfile: DockerFile
  #   volumes:
  #     - .:/prosinic_backend_core
  #   restart: always
  #   ports:
  #     - "8000:8000"
  #   networks:
  #     - prosonic_network
  #     - nginx_netword
  prosonic_postgresql:
    image: postgres:14.4
    container_name: prosonic_postgresql
    volumes:
      - prosonic_postgresql:/var/lib/postgresql/data
    restart: always
    env_file: .env
    ports:
      - "5432:5432"
    networks:
      - postgres_network


volumes:
  prosonic_postgresql:
    external: true 
networks:
  postgres_network:
    external: true 